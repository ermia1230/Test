import { vi, describe, it, expect, afterEach} from "vitest";

vi.mock("mobx-react-lite");
import {observer} from "mobx-react-lite";
observer.mockImplementation(function(x){ return x; }) ;

import {nativeCustomEvents} from "./nativeCustomEvents";
import {nativeRender} from "./nativeRender";
import { runCustomEventTests } from "./tw2.5.2-search.handleCustomEvents";

// re-run these tests if the react search presenter changes, or the native search views
import.meta.glob("/src/reactjs/searchPresenter.*sx")
import.meta.glob("/src/native-views/searchFormView.*sx")
import.meta.glob("/src/native-views/searchResultsView.*sx")

describe.for([{   name:"React Native",    render: nativeRender, path:"reactjs"}])
    ("", async function(framework){
    runCustomEventTests(framework, nativeCustomEvents);
});
