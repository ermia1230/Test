import { vi, describe, it, expect, afterEach} from "vitest";

vi.mock("mobx-react-lite");
import {observer} from "mobx-react-lite";
observer.mockImplementation(function(x){ return x; }) ;

import {nativeCustomEvents} from "./nativeCustomEvents";
import {nativeRender} from "./nativeRender";
import { runCustomEventTests } from "./tw1.5-10.handleCustomEvents";

// re-run these tests if anything happens to reactjs/sidebarPresenter or to the native sidebarView
import.meta.glob("/src/reactjs/sidebarPresenter.*sx")
import.meta.glob("/src/native-views/sidebarView.*sx")

describe.for([
	{   name:"React Native",	render: nativeRender,	path:"reactjs"},
])("", async function(framework){
	runCustomEventTests(framework, nativeCustomEvents)
});
