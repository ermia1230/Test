import {vi} from "vitest";

import {createElement, Fragment} from "react";


import {render as native_render} from "@testing-library/react-native";

export function nativeRender(component, props){
    window.React={createElement, Fragment};
    return native_render(createElement(component, props));
}

